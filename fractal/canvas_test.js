var chai = require('chai')
var expect = chai.expect

var Fractal = require('../canvas')

describe('canvas', function() {
  describe('prints a canvas with the correct attributes', function() {
    beforeEach(function() {
      this.canvas= new Canvas()
      console.log('making a canvas')
    })

    it('prints the canvas', function() {
      expect(this.angle).to.equal()
    })

  })
})

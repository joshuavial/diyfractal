var chai = require('chai')
var expect = chai.expectcd ..cd

var Fractal = require('../Changetree')

describe('changeTree', function() {
  describe('prints a new tree with the correct attributes', function() {
    beforeEach(function() {
      this.changeTree = new Changetree()
      console.log('changing my tree')
    })

    it('prints a new tree with correct angle ', function() {


    })

    it('prints a new tree with the correct depth', function() {

    })
  })
})
